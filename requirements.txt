# ─────────────────────────────────────────────────────────────────────────────
# Authoritative PINNED runtime dependencies for Wazuh MCP.
#
# This file is now safe to install from directly:
#
#   pip install -r requirements.txt        # runtime only
#   pip install -e .                        # same deps via pyproject.toml (ranges)
#   pip install -r requirements.lock        # full pinned closure incl. transitives + dev
#
# Version ranges (with upper bounds) live in pyproject.toml; the exact pins below
# and the complete transitive closure in requirements.lock are what CI's pip-audit
# and reproducible Docker/production installs consume. Regenerate after changing
# pyproject.toml dependencies.
# ─────────────────────────────────────────────────────────────────────────────
mcp==1.27.1
httpx==0.28.1
python-dotenv==1.2.2
uvicorn==0.47.0
starlette==1.0.1
structlog==25.5.0
defusedxml==0.7.1
prometheus-client==0.25.0
PyYAML==6.0.3
